---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "confluent_switchover_endpoint Resource - terraform-provider-confluent"
subcategory: ""
description: |-
  
---

# confluent_switchover_endpoint Resource

[![Preview](https://img.shields.io/badge/Lifecycle%20Stage-Preview-%2300afba)](https://docs.confluent.io/cloud/current/api.html#section/Versioning/API-Lifecycle-Policy)

!> **Warning:** Switchover is a preview feature. Preview features are introduced to gather customer feedback and may change based on that feedback. They are intended for evaluation and non-production use only, and are not covered by Confluent SLAs. Preview features may be discontinued at any time.

`confluent_switchover_endpoint` provides a switchover endpoint resource that models a sticky disaster recovery (DR) endpoint bound to a `confluent_switchover_pair`. The endpoint follows the pair's active member so that clients keep a stable connection target across a failover.

## Example Usage

```terraform
provider "confluent" {
  cloud_api_key    = var.confluent_cloud_api_key    # optionally use CONFLUENT_CLOUD_API_KEY env var
  cloud_api_secret = var.confluent_cloud_api_secret # optionally use CONFLUENT_CLOUD_API_SECRET env var
}

resource "confluent_switchover_endpoint" "example" {
  display_name = "prod-kafka-dr-endpoint"

  # The pair is referenced by its CRN, which carries the pair's environment.
  parent_resource_crn = "crn://confluent.cloud/organization=org-abc/environment=env-abc123/switchover-pair=sw-abc123"

  endpoints {
    name = "west-platt"
    endpoint_filter {
      type        = "private"
      network_crn = "crn://confluent.cloud/organization=org-abc/environment=env-abc123/network=n-west01"
    }
  }

  endpoints {
    name = "east-platt"
    endpoint_filter {
      type        = "private"
      network_crn = "crn://confluent.cloud/organization=org-abc/environment=env-def456/network=n-east01"
    }
  }
}
```

<!-- schema generated by tfplugindocs -->
## Argument Reference

The following arguments are supported:

- `display_name` - (Required String) A human-readable name for the switchover endpoint.
- `parent_resource_crn` - (Required String) The CRN of the switchover pair this endpoint is bound to, for example, `crn://confluent.cloud/organization=org-abc/environment=env-abc123/switchover-pair=sw-abc123`. The CRN carries the pair's environment.
- `target` - (Optional String) The name of the endpoint that should be active. For stateful pairs the control plane owns this value (it follows the pair's active member); on create it may be provided as an initial value. Must match one of the `endpoints[].name` values.
- `endpoints` (Required Configuration Block) The endpoint definitions, one per side (for example, west/east). Must contain exactly 2 entries. Each block supports the following:
  - `name` - (Required String) A logical name for this endpoint side (for example, `west-platt`), unique within the resource.
  - `endpoint_filter` (Required Configuration Block) Filter criteria that identify a network endpoint for this side of the pair. Supports the following:
    - `type` - (Required String) Whether the endpoint is `private` or `public`.
    - `network_crn` - (Optional String) The CRN of the network, for network-based private endpoints.
    - `access_point_crn` - (Optional String) The CRN of the network access point, for access-point (PNI) endpoints.

## Attributes Reference

In addition to the preceding arguments, the following attributes are exported:

- `id` - (Required String) The ID of the switchover endpoint, for example, `se-abc123`.
- `phase` - (Required String) The lifecycle phase of the switchover endpoint.
- `endpoints` (Required Configuration Block) In addition to the arguments above, each endpoint exports the following:
  - `hostname` - (Required String) The resolved hostname for this endpoint.
  - `cloud` - (Required String) The cloud provider this endpoint resolves to.
  - `region` - (Required String) The cloud region this endpoint resolves to.
  - `connection_type` - (Required String) The connection type this endpoint resolves to.

## Import

-> **Note:** `CONFLUENT_CLOUD_API_KEY` and `CONFLUENT_CLOUD_API_SECRET` environment variables must be set before importing a switchover endpoint.

You can import a switchover endpoint by using the parent resource CRN and switchover endpoint ID, in the format `<Parent Resource CRN>/<Switchover Endpoint ID>`. The following example shows how to import a switchover endpoint:

```shell
$ export CONFLUENT_CLOUD_API_KEY="<cloud_api_key>"
$ export CONFLUENT_CLOUD_API_SECRET="<cloud_api_secret>"
$ terraform import confluent_switchover_endpoint.example crn://confluent.cloud/organization=org-abc/environment=env-abc123/switchover-pair=sw-abc123/se-abc123
```

!> **Warning:** Do not forget to delete terminal command history afterwards for security purposes.
