---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "confluent_switchover_endpoint Data Source - terraform-provider-confluent"
subcategory: ""
description: |-
  
---

# confluent_switchover_endpoint Data Source

[![General Availability](https://img.shields.io/badge/Lifecycle%20Stage-General%20Availability-%2345c6e8)](https://docs.confluent.io/cloud/current/api.html#section/Versioning/API-Lifecycle-Policy)

`confluent_switchover_endpoint` describes a SwitchoverEndpoint data source.

## Example Usage

```terraform
provider "confluent" {
  cloud_api_key    = var.confluent_cloud_api_key    # optionally use CONFLUENT_CLOUD_API_KEY env var
  cloud_api_secret = var.confluent_cloud_api_secret # optionally use CONFLUENT_CLOUD_API_SECRET env var
}

data "confluent_switchover_endpoint" "example" {
  id = "swi-abc123"
  environment {
    id = "env-xyz456"
  }
}

output "example" {
  value = data.confluent_switchover_endpoint.example
}
```

<!-- schema generated by tfplugindocs -->
## Argument Reference

The following arguments are supported:

- `id` - (Required String) The ID of the SwitchoverEndpoint, for example, `swi-abc123`.
- `environment` - (Required Configuration Block) Supports the following:
    - `id` - (Required String) The ID of the Environment that the SwitchoverEndpoint belongs to, for example, `env-xyz456`.

## Attributes Reference

In addition to the preceding arguments, the following attributes are exported:

- `display_name` - (String) A human-readable name for the switchover endpoint.
- `dr_endpoint` - (String) The sticky global DR endpoint hostname that clients connect to. This is a server-computed, read-only attribute (the backend sets it and exposes it on the spec, consistent with how other Confluent resources expose computed endpoints, e.g. `cmk.v2.Cluster`). It follows the pattern `glkc-$switchoverPairId-$switchoverEndpointId.global.confluent.cloud`.
- `endpoints` - (String) The endpoint definitions, one per side (e.g. west/east). Must contain exactly 2 entries.
- `switchover_pair` - (Configuration Block) The switchover pair this endpoint is bound to. Supports the following:
    - `id` - (String) The ID of the Switchover Pair.
- `target` - (String) The name of the endpoint that should be active. For stateful pairs the control plane owns this value (it follows the pair's active member) and customers cannot set it directly; on create it may be provided as an initial value. Must match one of `endpoints[].name`.
- `api_version` - (String) API Version defines the schema version of this representation of a resource.
- `kind` - (String) Kind defines the object this REST resource represents.
- `resource_name` - (String) The Confluent Resource Name of the resource.
