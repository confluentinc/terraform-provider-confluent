// Copyright 2026 Confluent Inc. All Rights Reserved.
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

// Code generated by cli-terraform-generator; DO NOT EDIT.

package provider

import (
	"context"
	"fmt"
	"io/ioutil"
	"net/http"
	"testing"

	"github.com/hashicorp/terraform-plugin-sdk/v2/helper/resource"
	"github.com/hashicorp/terraform-plugin-sdk/v2/terraform"
	"github.com/walkerus/go-wiremock"
)

const (
	scenarioStateGroupMappingHasBeenCreated = "The new group mapping has been just created"
	scenarioStateGroupMappingHasBeenUpdated = "The group mapping has been just updated"
	scenarioStateGroupMappingHasBeenDeleted = "The group mapping has been deleted"
	groupMappingScenarioName                = "confluent_group_mapping Resource Lifecycle"
	groupMappingId                          = "group-f3a90de"
)

func TestAccIamGroupMapping(t *testing.T) {
	ctx := context.Background()

	wiremockContainer, err := setupWiremock(ctx)
	if err != nil {
		t.Fatal(err)
	}
	defer wiremockContainer.Terminate(ctx)

	mockServerUrl := wiremockContainer.URI
	wiremockClient := wiremock.NewClient(mockServerUrl)
	// nolint:errcheck
	defer wiremockClient.Reset()

	// nolint:errcheck
	defer wiremockClient.ResetAllScenarios()

	createGroupMappingResponse, _ := ioutil.ReadFile("../testdata/group_mapping/create_group_mapping.json")
	createGroupMappingStub := wiremock.Post(wiremock.URLPathEqualTo("/iam/v2/sso/group-mappings")).
		InScenario(groupMappingScenarioName).
		WhenScenarioStateIs(wiremock.ScenarioStateStarted).
		WillSetStateTo(scenarioStateGroupMappingHasBeenCreated).
		WillReturn(
			string(createGroupMappingResponse),
			contentTypeJSONHeader,
			http.StatusCreated,
		)
	_ = wiremockClient.StubFor(createGroupMappingStub)

	readCreatedGroupMappingResponse, _ := ioutil.ReadFile("../testdata/group_mapping/read_created_group_mapping.json")
	_ = wiremockClient.StubFor(wiremock.Get(wiremock.URLPathEqualTo(fmt.Sprintf("/iam/v2/sso/group-mappings/%s", groupMappingId))).
		InScenario(groupMappingScenarioName).
		WhenScenarioStateIs(scenarioStateGroupMappingHasBeenCreated).
		WillReturn(
			string(readCreatedGroupMappingResponse),
			contentTypeJSONHeader,
			http.StatusOK,
		))

	readUpdatedGroupMappingResponse, _ := ioutil.ReadFile("../testdata/group_mapping/read_updated_group_mapping.json")
	patchGroupMappingStub := wiremock.Patch(wiremock.URLPathEqualTo(fmt.Sprintf("/iam/v2/sso/group-mappings/%s", groupMappingId))).
		InScenario(groupMappingScenarioName).
		WhenScenarioStateIs(scenarioStateGroupMappingHasBeenCreated).
		WillSetStateTo(scenarioStateGroupMappingHasBeenUpdated).
		WillReturn(
			string(readUpdatedGroupMappingResponse),
			contentTypeJSONHeader,
			http.StatusOK,
		)
	_ = wiremockClient.StubFor(patchGroupMappingStub)

	_ = wiremockClient.StubFor(wiremock.Get(wiremock.URLPathEqualTo(fmt.Sprintf("/iam/v2/sso/group-mappings/%s", groupMappingId))).
		InScenario(groupMappingScenarioName).
		WhenScenarioStateIs(scenarioStateGroupMappingHasBeenUpdated).
		WillReturn(
			string(readUpdatedGroupMappingResponse),
			contentTypeJSONHeader,
			http.StatusOK,
		))

	readDeletedGroupMappingResponse, _ := ioutil.ReadFile("../testdata/group_mapping/read_deleted_group_mapping.json")
	_ = wiremockClient.StubFor(wiremock.Get(wiremock.URLPathEqualTo(fmt.Sprintf("/iam/v2/sso/group-mappings/%s", groupMappingId))).
		InScenario(groupMappingScenarioName).
		WhenScenarioStateIs(scenarioStateGroupMappingHasBeenDeleted).
		WillReturn(
			string(readDeletedGroupMappingResponse),
			contentTypeJSONHeader,
			http.StatusForbidden,
		))

	deleteGroupMappingStub := wiremock.Delete(wiremock.URLPathEqualTo(fmt.Sprintf("/iam/v2/sso/group-mappings/%s", groupMappingId))).
		InScenario(groupMappingScenarioName).
		WhenScenarioStateIs(scenarioStateGroupMappingHasBeenUpdated).
		WillSetStateTo(scenarioStateGroupMappingHasBeenDeleted).
		WillReturn(
			"",
			contentTypeJSONHeader,
			http.StatusNoContent,
		)
	_ = wiremockClient.StubFor(deleteGroupMappingStub)
	displayName := "Application Developers"
	description := "Admin access to production environment for Engineering"
	filter := "\"kafka\" in groups && \"all\" in groups || \"everyone\" in groups"
	// in order to test tf update (step #3)
	displayNameUpdated := "Application Developers_updated"
	descriptionUpdated := "Admin access to production environment for Engineering_updated"
	filterUpdated := "\"kafka\" in groups && \"all\" in groups || \"everyone\" in groups_updated"
	groupMappingResourceLabel := "test_group_mapping_resource_label"
	fullGroupMappingResourceLabel := fmt.Sprintf("confluent_group_mapping.%s", groupMappingResourceLabel)

	resource.Test(t, resource.TestCase{
		PreCheck:          func() { testAccPreCheck(t) },
		ProviderFactories: testAccProviderFactories,
		CheckDestroy:      testAccCheckIamGroupMappingDestroy,
		// https://www.terraform.io/docs/extend/testing/acceptance-tests/teststep.html
		// https://www.terraform.io/docs/extend/best-practices/testing.html#built-in-patterns
		Steps: []resource.TestStep{
			{
				Config: testAccCheckIamGroupMappingConfig(mockServerUrl, groupMappingResourceLabel, displayName, description, filter),
				Check: resource.ComposeTestCheckFunc(
					testAccCheckIamGroupMappingExists(fullGroupMappingResourceLabel),
					resource.TestCheckResourceAttr(fullGroupMappingResourceLabel, "id", groupMappingId),
					resource.TestCheckResourceAttr(fullGroupMappingResourceLabel, "display_name", displayName),
					resource.TestCheckResourceAttr(fullGroupMappingResourceLabel, "description", description),
					resource.TestCheckResourceAttr(fullGroupMappingResourceLabel, "filter", filter),
				),
			},
			{
				// https://www.terraform.io/docs/extend/resources/import.html
				ResourceName:      fullGroupMappingResourceLabel,
				ImportState:       true,
				ImportStateVerify: true,
			},
			{
				Config: testAccCheckIamGroupMappingConfig(mockServerUrl, groupMappingResourceLabel, displayNameUpdated, descriptionUpdated, filterUpdated),
				Check: resource.ComposeTestCheckFunc(
					testAccCheckIamGroupMappingExists(fullGroupMappingResourceLabel),
					resource.TestCheckResourceAttr(fullGroupMappingResourceLabel, "id", groupMappingId),
					resource.TestCheckResourceAttr(fullGroupMappingResourceLabel, "display_name", displayNameUpdated),
					resource.TestCheckResourceAttr(fullGroupMappingResourceLabel, "description", descriptionUpdated),
					resource.TestCheckResourceAttr(fullGroupMappingResourceLabel, "filter", filterUpdated),
				),
			},
			{
				ResourceName:      fullGroupMappingResourceLabel,
				ImportState:       true,
				ImportStateVerify: true,
			},
		},
	})

	checkStubCount(t, wiremockClient, createGroupMappingStub, "POST /iam/v2/sso/group-mappings", expectedCountOne)
	checkStubCount(t, wiremockClient, patchGroupMappingStub, fmt.Sprintf("PATCH /iam/v2/sso/group-mappings/%s", groupMappingId), expectedCountOne)
	checkStubCount(t, wiremockClient, deleteGroupMappingStub, fmt.Sprintf("DELETE /iam/v2/sso/group-mappings/%s", groupMappingId), expectedCountOne)
}

func testAccCheckIamGroupMappingDestroy(s *terraform.State) error {
	c := testAccProvider.Meta().(*Client)
	// Loop through the resources in state, verifying each group mapping is destroyed
	for _, rs := range s.RootModule().Resources {
		if rs.Type != "confluent_group_mapping" {
			continue
		}
		deletedGroupMappingId := rs.Primary.ID
		req := c.ssoV2Client.GroupMappingsIamV2SsoApi.GetIamV2SsoGroupMapping(c.ssoV2ApiContext(context.Background()), deletedGroupMappingId)
		deletedGroupMapping, response, err := req.Execute()
		if response != nil && (response.StatusCode == http.StatusForbidden || response.StatusCode == http.StatusNotFound) {
			// If the error is equivalent to http.StatusNotFound, the group mapping is destroyed.
			return nil
		} else if err == nil && deletedGroupMapping.Id != nil {
			// Otherwise return the error
			if *deletedGroupMapping.Id == rs.Primary.ID {
				return fmt.Errorf("group mapping (%q) still exists", rs.Primary.ID)
			}
		}
		return err
	}
	return nil
}

func testAccCheckIamGroupMappingConfig(mockServerUrl, groupMappingResourceLabel string, displayName string, description string, filter string) string {
	return fmt.Sprintf(`
	provider "confluent" {
		endpoint = "%s"
	}
	resource "confluent_group_mapping" "%s" {
		display_name = %q
		description = %q
		filter = %q
	}
	`, mockServerUrl, groupMappingResourceLabel, displayName, description, filter)
}

func testAccCheckIamGroupMappingExists(n string) resource.TestCheckFunc {
	return func(s *terraform.State) error {
		rs, ok := s.RootModule().Resources[n]

		if !ok {
			return fmt.Errorf("%s group mapping has not been found", n)
		}

		if rs.Primary.ID == "" {
			return fmt.Errorf("ID has not been set for %s group mapping", n)
		}

		return nil
	}
}
