name: terraform-provider-confluent
contact:
  name: api
  jira_project_key: CLI
  email: api-team@confluent.io
  slack_team_channel: api-eng
  slack_oncall_channel: terraform
lang: go
lang_version: 1.24.4
git:
  enable: true
github:
  enable: true
semaphore:
  enable: true
  pipeline_enable: false
  branches:
    - master
    - /^v\d+\.\d+\.x$/
make:
  enable: false
sonarqube:
  enable: true
  languages:
    - go
  main_branch: master
